<aside class="main-sidebar">
    <!-- sidebar: style can be found in sidebar.less -->
    <section class="sidebar">
        <!-- sidebar menu: : style can be found in sidebar.less -->
        <ul class="sidebar-menu" data-widget="tree">
            <li class="header">NAVIGATION</li>
            <% if (typeof(activeDasboard) != 'undefined') { %>
                <li class="active">
            <% } else { %>
                <li>
            <% } %>
                <a href="/admin">
                    <i class="fa fa-dashboard"></i>
                    <span>Dashboard</span>
                </a>
            </li>
            <% if (typeof(activeCategory) != 'undefined') { %>
                <li class="treeview active">
            <% } else { %>
                <li class="treeview">
            <% } %>
                <a href="javascript:void(0)">
                <i class="fa fa-th-list"></i>
                <span>Category</span>
                <span class="pull-right-container">
                <i class="fa fa-angle-left pull-right"></i>
                </span>
                </a>
                <ul class="treeview-menu">
                    <li><a href="/admin/categories"><i class="fa fa-circle-o"></i>Category list</a></li>
                    <li><a href="/admin/categories/create"><i class="fa fa-circle-o"></i>Create category</a></li>
                </ul>
            </li>
            <% if (typeof(activeRequest) != 'undefined') { %>
                <li class="active">
            <% } else { %>
                <li>
            <% } %>
                <a href="/admin/waiting-request-edit-book">
                    <i class="fa fa-paper-plane-o" aria-hidden="true"></i>
                    <span>Waiting request</span>
                </a>
            </li>
            <% if (typeof(activeUser) != 'undefined') { %>
                <li class="active">
            <% } else { %>
                <li>
            <% } %>
                <a href="/admin/users">
                    <i class="fa fa-user-o" aria-hidden="true"></i>
                    <span>User List</span>
                </a>
            </li>
        </ul>
    </section>
    <!-- /.sidebar -->
</aside>
